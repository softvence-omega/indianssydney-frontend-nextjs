import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com","flagcdn.com"], // Allow images from Unsplash
  },
};

export default nextConfig;
